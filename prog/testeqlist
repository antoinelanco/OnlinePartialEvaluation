(
[
"findfun",
(
["el","tab"],
  if (prim equal [var "tab", const(tval[])] )
    (exception)
    (if (prim equal [var "el", hd(var "tab")] )
    (const(bval true))
    (apply "findfun" [var "el" ,tl(var "tab")] ) )

)],
  apply "findfun" [var "x", var "y"]
)
